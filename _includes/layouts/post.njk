---
layout : layouts/base.njk
---

<div class="container col-xxl-8 px-4">
    <div class="row flex-lg-row-reverse align-items-center g-5 py-5">
      <div class="col-10 col-sm-8 col-lg-6">
        <img src="{{ featuredImage |url }}" class="d-block mx-lg-auto img-fluid" alt="{{ featuredImage }}" width="700" height="500" loading="lazy">
      </div>
      <div class="col-lg-6">
        <h1 class="h2 fw-bold text-body-emphasis-white lh-1 mb-3 bg-success text-white">{{ title }}</h1>
        <h5> Publié le <time>{{ date | date }} </time></h5>
        <p class="lead">{{ content | safe }}</p>
        <div class="d-grid gap-2 d-md-flex justify-content-md-start">
        </div>
      </div>
    </div>
  </div>